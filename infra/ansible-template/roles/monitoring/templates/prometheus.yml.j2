global:
  scrape_interval: 15s

scrape_configs:
  - job_name: prometheus
    static_configs:
      - targets: ["localhost:9090"]

  - job_name: nodes
    static_configs:
      - targets:
{% for target in monitoring_node_targets %}
          - "{{ target }}"
{% endfor %}
